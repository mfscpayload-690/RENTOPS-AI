package utils;

/**
 * Helper method to create a placeholder ImageIcon for when an image cannot be loaded
 * @param maxW Desired width
 * @param maxH Desired height
 * @return A placeholder ImageIcon
 */
public static ImageIcon createPlaceholderIcon(int maxW, int maxH) {
    return new ImageIcon(createPlaceholderImage(maxW, maxH));
}